let enemyArray = [];

////for loop needed here to push lots to an array
/*enemyArray.push(new moveableStaticSprite(
    30,
    100,
    30,
    30,
    '../images/cat.png',
    'myCanvas'
));
enemyArray[0].currentDirection = 'right';

setInterval(() => {

    // for eachloop if you call enemy ship as the first variable it will stop you needing to rename

    var dx = 10;
    var dy = 10;
    let enemyShip = enemyArray[0]
    if (enemyShip.currentDirection === 'left') {
        enemyShip.moveSprite(enemyShip.x - dx, enemyShip.y);
        if (enemyShip.x <= 0) {
            enemyShip.moveSprite(enemyShip.x + dx, enemyShip.y + dy);
            enemyShip.currentDirection = 'right';
        }
    }
    if (enemyShip.currentDirection === 'right') {
        enemyShip.moveSprite(enemyShip.x + dx, enemyShip.y);

        if (enemyShip.x >= 600) {
            enemyShip.moveSprite(enemyShip.x - dx, enemyShip.y + dy);
            enemyShip.currentDirection = 'left';
        }
    }
    if (enemyShip.y >= 500) {
        // alert("Game Over!");
    }
}, 50);
*/